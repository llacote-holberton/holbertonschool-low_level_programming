#include <stdio.h>

/**
 * main - Displays a simple static text.
 *
 * Description: displays a fixed string using printf.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char *text = "with proper grammar, but the outcome is a piece of art,\n";

	printf("%s", text);

	return (0);

}
